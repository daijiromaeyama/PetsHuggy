nav.navbar.navbar-devise
  = render 'partial/navbar'

.container
  .col-md-3
    = render 'partial/verticalnavbar'

  .col-md-9
    .panel.panel-default
      .panel-heading
        .text-center
          span[style="font-size:24px;"]
            | 受取講座情報の編集

      .panel-body
        - if @listing.active
          | リスティングを非公開にしてから、編集してください。
        - elsif @user.connected?
          = render 'stripe/setting'
        - else
          = render 'stripe/connect'
